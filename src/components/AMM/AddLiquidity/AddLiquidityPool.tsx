import { IPool } from "../../../Models"

const AddLiquidityPool:  React.FC<{pool: IPool, setState: React.Dispatch<string>}> = ({pool, setState}) => {
    return <></>
}

export default AddLiquidityPool